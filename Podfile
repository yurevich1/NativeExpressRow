# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NativeExpressRow' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for NativeExpressRow
  pod 'Eureka'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  target 'NativeExpressRowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NativeExpressRowUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
